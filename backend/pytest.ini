[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -m "not integration"
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: Integration tests
    unit: Unit tests
    slow: Slow tests
