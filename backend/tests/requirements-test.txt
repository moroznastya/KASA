# ── Залежності для тестування Kasa POS ──────────────────────────────────────
# Встановлення: pip install -r requirements-test.txt

# Основний фреймворк тестування
pytest>=8.0.0
pytest-asyncio>=0.23.0

# HTTP клієнт для тестування FastAPI (заміна TestClient)
httpx>=0.27.0

# Фабрики тестових даних
factory_boy>=3.3.0

# Додаткові утиліти
pytest-cov>=5.0.0       # Покриття коду
pytest-xdist>=3.0.0     # Паралельний запуск
aiosqlite>=0.20.0
