# ── Основні залежності ─────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── База даних ─────────────────────────────────
sqlalchemy>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0

# ── Аутентифікація ─────────────────────────────
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# ── Rate Limiting ───────────────────────────────
slowapi>=0.1.9

# ── Інше ───────────────────────────────────────
python-multipart>=0.0.9

# ── OCR / Gemini API ───────────────────────────
google-genai>=1.0.0

# ── Штрих-коди для етикеток ──────────────────────
python-barcode>=0.16.0

# ── QR-коди для етикеток ────────────────────────
qrcode[pil]>=8.0.0

# ── ПРРО (gRPC клієнт для фіскалізації чеків) ──────────────
grpcio>=1.60.0
grpcio-tools>=1.60.0
cryptography>=42.0.0
lxml>=5.0.0
signxml>=4.0.0
pyjks>=20.0.0
