# ── Python ─────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg
.venv/
venv/
env/
*.whl
*.manifest
*.spec

# ── Virtual Environment ────────────────────────
backend/venv/
backend/.venv/

# ── Node.js / Frontend ─────────────────────────
node_modules/
frontend/node_modules/
frontend/dist/
frontend/build/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Tauri ──────────────────────────────────────
frontend/src-tauri/target/
frontend/src-tauri/bin/

# ── Tauri Updater: ключі підпису (СЕКРЕТИ!) ───
# Приватний ключ + папка ключів НЕ повинні потрапити в git!
frontend/src-tauri/.tauri/
frontend/src-tauri/*.key
frontend/src-tauri/*.key.pub
frontend/src-tauri/.env

# ── Environment Variables ──────────────────────
.env
.env.local
.env.*.local
backend/.env
frontend/.env

# ── IDE / Editor ───────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── Build Artifacts ────────────────────────────
*.log
*.pid
*.seed
*.pid.lock
*.sqlite3
coverage/
.coverage
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# ── Docker ─────────────────────────────────────
docker-compose.override.yml

# ── Alembic (keep versions, ignore runtime) ───
alembic/versions/*.pyc

# ── OS Files ───────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Secrets
backend/keys.txt

# ── Local SQLite artifacts (PostgreSQL-проєкт) ──
*.db
*.db-journal
*.sqlite
*.sqlite3

# ── ПРРО: електронні ключі (КЕП) та сертифікати ──
certs/
*.pem
*.pfx
*.p12
*.jks
*.keystore
*.key
Key-6.dat
Cert-6.dat

# ── ПРРО: master-ключ та сховище налаштувань ключа (секрети) ──
.prro_master.key
.prro_keystore.json

# ІІТ SDK (крипто-ядро EUSignCP) — встановлюється scripts/setup_iit_sdk.sh
backend/vendor/iit-sdk/etc/
backend/vendor/iit-sdk/opt/iit/eu/sw/euscpnmh

# ── Packaging Artifacts (Linux) ─────────────
*.deb
*.rpm
*.AppImage
*.pkg.tar.zst

# ── Secrets / Keys (глобально) ────────────────
keys.txt
*.pem
*.p12
*.pfx

# PostgreSQL binaries download cache
.cache/
